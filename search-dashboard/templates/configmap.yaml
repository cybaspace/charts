kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "search-dashboard-frontend.fullname" . }}-cfgmap
  labels:
  {{- include "search-dashboard-frontend.labels" . | nindent 4 }}
data:
  config.json: |-
    {
      "API_URL": "https://search-dashboard-api.{{ .Values.cluster.domain }}"
    }